namespace GravyIrc
{
    public enum IrcCommand
    {
        UNKNOWN,
        NICK,
        MODE,
        QUIT,
        JOIN,
        PART,
        TOPIC,
        INVITE,
        KICK,
        PRIVMSG,
        NOTICE,
        PING,
        ERROR
    }
}
